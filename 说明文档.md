# 借贷还款目标管理系统 - 说明文档

---

## 一、项目规划

### 1.1 项目背景
借贷还款目标管理系统是一个帮助用户管理多笔债务、追踪还款进度、智能规划还款路线的工具应用。支持债务重组策略模拟，帮助用户在复杂债务结构下制定最优还款方案。

### 1.2 核心功能模块
| 模块 | 功能描述 | 优先级 |
|------|----------|--------|
| 债务管理 | 记录多笔借款信息（金额、利率、期限、还款日、最低还款额） | 高 |
| 资产管理 | 记录当前可用资金、固定资产等 | 高 |
| 收入管理 | 记录每月进账金额、固定支出、可用于还款的金额 | 中 |
| 智能还款规划 | 根据目标时间、利率等策略生成还款计划 | 高 |
| 债务重组模拟 | 债务转移、借新还旧等策略模拟与分析 | 高 |
| 可视化展示 | 还款路线图、债务变化趋势图 | 中 |

### 1.3 技术架构
- **前端框架**: React + TypeScript
- **UI组件库**: Ant Design
- **图表库**: ECharts
- **数据存储**: SQLite (sql.js - WebAssembly)
- **状态管理**: React Context
- **构建工具**: Vite
- **样式方案**: CSS Modules + SCSS

### 1.4 项目时间规划
| 阶段 | 内容 | 预计完成时间 |
|------|------|------------|
| 第一阶段 | 项目搭建 + 基础模块 | 2026-06-26 |
| 第二阶段 | 智能规划引擎 + 债务重组 | 2026-06-26 |
| 第三阶段 | 可视化 + 测试优化 | 2026-06-27 |

---

## 二、实施方案

### 2.1 技术方案

#### 2.1.1 数据模型
```typescript
// 债务信息
interface Debt {
  id: string;
  name: string;           // 债务名称（包含具体平台/产品名称）
  type: string;           // 债务类型（信用卡/网贷/贷款）
  remainingAmount: number;   // 当前剩余金额
  interestRate?: number;    // 年利率（可选）
  dueDate: number;        // 每月还款日
  createdAt: string;
  note?: string;
}

// 资产信息
interface Asset {
  id: string;
  name: string;
  type: string;
  amount: number;
  liquidity: 'high' | 'medium' | 'low'; // 流动性
}

// 收入信息
interface Income {
  monthlyIncome: number;   // 月收入
  monthlyExpense: number;    // 月支出
  availableForRepayment: number; // 可用于还款金额
}

// 还款计划
interface RepaymentPlan {
  months: RepaymentMonth[];
  totalInterest: number;
  totalPayment: number;
  payoffDate: string;
  strategy: string;
}
```

#### 2.1.2 债务类型预设
- 信用卡
- 网贷
- 贷款

> 注：具体平台/产品名称写入债务名称字段（如"蚂蚁借呗"、"京东白条"等）

#### 2.1.3 还款策略
1. **雪崩法（Avalanche）**: 优先偿还利率最高的债务
2. **雪球法（Snowball）**: 优先偿还余额最小的债务
3. **最低还款法**: 只还最低还款额，剩余理财
4. **债务重组**: 借新还旧、余额转移等策略模拟

### 2.2 文件结构
```
LoanRepayment/
├── public/
│   └── sql-wasm.wasm            # SQLite WASM 文件
├── src/
│   ├── components/              # 组件目录
│   │   ├── DebtManager/         # 债务管理
│   │   ├── AssetManager/        # 资产管理
│   │   ├── IncomeManager/       # 收入管理
│   │   ├── PlanEngine/          # 还款规划引擎
│   │   ├── Restructure/         # 债务重组
│   │   └── Dashboard/           # 仪表盘/可视化
│   ├── services/
│   │   └── database.ts        # SQLite 数据库服务层
│   ├── utils/                   # 工具函数
│   ├── types/                   # 类型定义
│   ├── context/                 # 状态管理
│   ├── App.tsx
│   └── main.tsx
├── package.json
├── vite.config.ts
└── tsconfig.json
```

---

## 三、进度记录

| 日期 | 任务 | 状态 | 说明 |
|------|------|------|------|
| 2026-06-26 | 项目启动 + 说明文档创建 | 已完成 | 创建项目说明文档，规划整体架构 |
| 2026-06-26 | 项目基础结构搭建 | 已完成 | Vite + React + TypeScript + Ant Design 项目搭建 |
| 2026-06-26 | 类型定义 + 工具函数 | 已完成 | 定义数据模型、还款规划引擎核心算法 |
| 2026-06-26 | 状态管理 Context | 已完成 | 实现全局状态管理与 localStorage 持久化 |
| 2026-06-26 | 债务管理模块 | 已完成 | 借款记录增删改查、类型分类 |
| 2026-06-26 | 资产管理模块 | 已完成 | 资产记录、流动性分级 |
| 2026-06-26 | 收入管理模块 | 已完成 | 月收入/支出设置、可还款金额计算 |
| 2026-06-26 | 智能还款规划引擎 | 已完成 | 雪崩法、雪球法、最低还款法，策略对比 |
| 2026-06-26 | 债务重组策略 | 已完成 | 最低还款滚动、余额转移、债务整合、借新还旧 |
| 2026-06-26 | 仪表盘总览 | 已完成 | 财务总览、风险评估、可视化图表 |
| 2026-06-26 | 构建测试 | 已完成 | TypeScript 检查通过、生产构建成功 |
| 2026-06-26 | 数据存储升级 | 已完成 | 从 localStorage 迁移到 SQLite (sql.js) |
| 2026-06-26 | 债务表单简化 | 已完成 | 去掉本金/最低还款字段，利率改为可选，类型简化为三种 |
