# 还款管家 - 扁平化优化方案 V2

## 一、设计理念

**核心原则**：去装饰、去阴影、去渐变，用色彩和排版承载信息层级。

### 扁平化设计规范调整

| 项目 | 当前 | 优化后 |
|------|------|--------|
| 卡片边框 | `1px solid #f0f0f0` | 无边框，改用背景色区分 |
| 卡片阴影 | hoverable 有阴影 | 无阴影，hover 仅变背景色 |
| 统计卡片 | 圆角8px + border + hover阴影 | 无border，浅色背景填充 |
| 图表 | 渐变area、borderRadius | 纯色填充，无圆角 |
| 进度条 | 默认样式 | 扁平色，无圆角 |
| 标题装饰 | 每处手写 `<span style={fontSize, fontWeight}>` | 抽取 `SectionCard` 统一封装 |
| 间距 | 混用16px/24px | 卡片间16px，区块间24px |

### 统一卡片标题

当前所有页面手写 `<span style={{ fontSize: FONT.h2, fontWeight: 600 }}>标题</span>`，改用 `SectionCard` 组件统一封装，消除重复代码。

---

## 二、冗余功能精简

### 2.1 Dashboard 页面精简

| 项目 | 问题 | 处理 |
|------|------|------|
| 高利率债务列表 | 与 DebtManager 表格重复 | 删除整个列表区块 |
| 关键指标 + 风险评估 | 两块内容信息重叠（都展示还款能力） | 合并为「财务健康度」单卡片 |
| 4个统计卡片 + 指标 | 统计卡片已含负债率，指标区又展示覆盖率 | 统计卡片保留4个核心，指标精简 |

**合并方案**：将「关键指标」和「风险评估」合并为一张「财务健康度」卡片，用简洁的指标行 + 风险标签展示。

### 2.2 DebtManager 精简

| 项目 | 问题 | 处理 |
|------|------|--------|
| 备注列 | 表格中显示 "-" 几乎无信息 | 移入展开行或删除 |
| 出账日 + 最迟还款日 | 两列占空间但信息密度低 | 合并为「还款日」单列 |
| 下载模板/导入/导出 | 三个按钮占行 | 合并为下拉菜单 `更多操作` |
| `importRef` | 声明未使用 | 删除 |
| `WalletOutlined`, `BankOutlined` | 导入未使用 | 删除 |

### 2.3 IncomeManager 精简

| 项目 | 问题 | 处理 |
|------|------|--------|
| 两个 Dashboard Progress | 占两行，信息密度低 | 改为紧凑的横条进度条 |
| 统计卡片 4 个 | "预计还清时间" 与 PlanEngine 重复 | 保留3个（月收入/月支出/可还款） |
| `Statistic` import | 导入未使用 | 删除 |
| 底部公式说明 | 信息冗余 | 合并到实时预览中 |

### 2.4 PlanEngine 精简

| 项目 | 问题 | 处理 |
|------|------|--------|
| 策略对比 Progress bar | 雪崩法始终100%，无对比意义 | 改为纯文字+数字对比表 |
| `RocketOutlined` | 导入未使用 | 删除 |
| 预计还清日期 | 用原始Card而非StatisticCard | 统一使用 StatisticCard |
| 目标时间设置 | 占半屏空间但交互简单 | 精简为单行 |

### 2.5 DebtRestructure 精简

| 项目 | 问题 | 处理 |
|------|------|--------|
| `rollForm`, `consolidationForm`, `transferForm` | 声明未使用 | 删除 |
| `RESTRUCTURE_TYPE_LABELS` | 导入未使用 | 删除 |
| `generateRepaymentPlan` | 导入未使用 | 删除 |
| `Select`, `Button` | 导入未使用 | 删除 |
| 借新还旧 Tab | 纯静态文字，4个嵌套Card | 改为简洁的信息卡片列表 |
| 借新还旧场景 | 4个内嵌Card信息冗余 | 精简为表格形式 |

### 2.6 AssetManager 精简

| 项目 | 问题 | 处理 |
|------|------|--------|
| `📦` emoji | 不专业 | 替换为 Ant Design 图标 |
| `BankOutlined` | 导入但仅用于一处 | 统一用 StatisticCard prefix |
| 流动性图标 `●◆▲` | 字符图标不统一 | 改用纯色 Tag |

---

## 三、各页面优化详情

### 3.1 Dashboard 优化

**改版前结构**（4 层）：
1. 4 个统计卡片
2. 关键指标 + 风险评估（2 列）
3. 债务分布饼图 + 资产分布饼图（2 列）
4. 高利率债务列表

**改版后结构**（3 层）：
1. 4 个统计卡片（扁平背景色填充）
2. 财务健康度（合并指标+风险）+ 资产负债概览饼图（2 列）
3. 还款时间线趋势图（全宽，替代债务分布+列表）

**财务健康度卡片**：用紧凑的指标行展示，每行一个指标，右侧带状态 Tag。

### 3.2 DebtManager 优化

- 表格列精简：合并出账日+最迟还款日 → 「还款日」
- 备注列删除，移入编辑 Modal
- 操作按钮：导入/导出/模板 → Dropdown 下拉菜单
- 使用率列：用扁平 Progress 替代 Tag

### 3.3 IncomeManager 优化

- 统计卡片：3 个（月收入/月支出/可还款金额）
- 删除两个 Dashboard Progress，改为表单下方的紧凑指标行
- 实时预览：改为带背景色的信息条

### 3.4 PlanEngine 优化

- 统计卡片：4 个统一用 StatisticCard
- 策略对比：改为紧凑的对比表格
- 目标时间：精简为单行 DatePicker + 结果标签
- 趋势图：去掉 areaStyle 渐变，纯线条

### 3.5 DebtRestructure 优化

- 删除所有未使用变量和导入
- 借新还旧 Tab：4 个嵌套 Card → 1 个简洁表格
- 参数设置区：统一紧凑布局
- 所有卡片标题统一使用 SectionCard

---

## 四、新增组件

### 4.1 SectionCard

统一卡片标题封装，消除重复的 `<span style={...}>` 代码：

```tsx
interface SectionCardProps {
  title: string;
  extra?: React.ReactNode;
  children: React.ReactNode;
}
// 自动应用 flat 设计：无边框、浅色标题
```

---

## 五、实施计划

| 优先级 | 任务 | 涉及文件 |
|--------|------|----------|
| P0 | 新增 SectionCard 组件 | src/components/Common/SectionCard.tsx |
| P0 | 更新 theme.ts 扁平化色值 | src/styles/theme.ts |
| P0 | 更新 StatisticCard 扁平化样式 | src/components/Common/StatisticCard.tsx |
| P0 | 更新 index.css 全局扁平化 | src/index.css |
| P1 | Dashboard 精简重构 | src/components/Dashboard/index.tsx |
| P1 | DebtManager 精简重构 | src/components/DebtManager/index.tsx |
| P1 | IncomeManager 精简重构 | src/components/IncomeManager/index.tsx |
| P1 | PlanEngine 精简重构 | src/components/PlanEngine/index.tsx |
| P1 | DebtRestructure 精简重构 | src/components/DebtRestructure/index.tsx |
| P1 | AssetManager 精简重构 | src/components/AssetManager/index.tsx |
| P2 | 构建验证 | npm run build |