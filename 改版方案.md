# 还款管家 - 完整改版方案

## 一、项目概述

**项目名称**：还款管家 (Loan Repayment Manager)
**技术栈**：React 18 + TypeScript + Vite + Ant Design 5 + ECharts + Express + SQLite
**功能模块**：财务总览、债务管理、资产管理、收入支出、还款规划、债务重组

---

## 二、当前问题分析

### 2.1 字体大小不统一

| 位置 | 当前字号 | 问题 |
|------|---------|------|
| 页面标题（Dashboard） | `<h2>`（约24px） | 与其他页面 `<h3>`（约20px）不一致 |
| 页面标题（其他页面） | `<h3>`（约20px） | 与 Dashboard 不一致 |
| App.tsx 顶栏标题 | `Title level={5}`（约17px） | 与页面标题层级不匹配 |
| 统计卡片数值 | 混用 `fontSize: 16` / `18` / 无设置 | 不统一 |
| 图表图例 | `fontSize: 11` | 过小 |
| 次要文字 | 混用 `fontSize: 12` / `13` | 不统一 |
| 表格内 Tag | 无固定字号 | 随默认 |

### 2.2 组件样式不统一

| 项目 | 问题 |
|------|------|
| Card 大小 | 混用 `size="small"` 和默认 `middle`，无统一规则 |
| 按钮样式 | 操作栏有的用图标+文字（AssetManager），有的只有图标（DebtManager） |
| 页面边距 | 部分页面 `marginBottom: 16`，部分 `marginBottom: 24` |
| 颜色使用 | 全局硬编码，无主题变量体系 |
| 空状态 | 纯文字提示，无统一空状态组件 |
| 页面布局 | 页面标题位置、副标题格式不统一 |

### 2.3 功能逻辑问题

| 问题 | 说明 |
|------|------|
| 收入支出实时保存 | `onValuesChange` 每改一个字段就触发保存，应改为 `onBlur` 或手动保存 |
| 债务管理表头重复 | "剩余金额" 和 "已用额度" 列显示相同数据，逻辑冗余 |
| 目标日期分析 | 默认无目标日期时显示空白，无引导提示 |
| 还款规划策略对比 | 没有自定义策略的调整入口 |
| 债务重组余额转移 | 自动筛选利率≥15%的债务，但用户无法手动调整 |

### 2.4 UI/UX 设计问题

| 问题 | 说明 |
|------|------|
| 移动端适配 | 部分页面在大屏小屏过渡不够平滑 |
| 加载状态 | 数据加载时无 Skeleton 或 Loading 指示器 |
| 操作反馈 | 删除/修改操作后缺少统一的 Toast 或动画反馈 |
| 颜色可读性 | 部分 `#999` 文字在浅色背景上对比度不足 |
| 交互细节 | 缺少 hover 效果、过渡动画等交互细节 |
| 品牌一致性 | 品牌色 #1890ff 使用分散，无统一的品牌视觉系统 |

---

## 三、统一设计规范（Design System）

### 3.1 字体大小体系

| 层级 | 用途 | 字号 | 字重 | 颜色 |
|------|------|------|------|------|
| H1 | 页面标题 | 20px | 600 | #1a1a1a |
| H2 | 卡片标题 | 16px | 600 | #1a1a1a |
| H3 | 区块子标题 | 14px | 600 | #333 |
| Body | 正文 | 14px | 400 | #333 |
| Body-Small | 辅助文字 | 13px | 400 | #666 |
| Caption | 标注/提示 | 12px | 400 | #999 |
| Statistic-Value | 统计数值 | 24px | 700 | 按语义 |
| Table-Header | 表头 | 14px | 600 | #333 |
| Table-Cell | 表格内容 | 13px | 400 | #333 |
| Tag | 标签 | 12px | 500 | 按类型 |

### 3.2 颜色体系

| 类别 | 色值 | 用途 |
|------|------|------|
| Primary | #1677ff | 主色、强调、链接 |
| Success | #52c41a | 正向、已还清、充足 |
| Warning | #faad14 | 警告、注意 |
| Danger | #ff4d4f | 高风险、逾期、不足 |
| Info | #1677ff | 信息提示 |
| Text-Primary | #1a1a1a | 主文字 |
| Text-Secondary | #666666 | 副文字 |
| Text-Tertiary | #999999 | 辅助文字 |
| Border | #f0f0f0 | 边框、分割线 |
| Bg-Page | #f5f5f5 | 页面背景 |
| Bg-Card | #ffffff | 卡片背景 |

### 3.3 间距体系

使用 Ant Design 的 4px 网格体系：

| 层级 | 值 | 用途 |
|------|----|------|
| XS | 4px | 图标与文字间距 |
| SM | 8px | 紧凑元素间距 |
| MD | 12px | 标签间距、内边距 |
| LG | 16px | 组件间距、卡片内边距 |
| XL | 24px | 区块间距 |
| XXL | 32px | 大区块间距 |

### 3.4 圆角体系

| 层级 | 值 | 用途 |
|------|----|------|
| SM | 4px | 小标签 |
| MD | 6px | 按钮、输入框 |
| LG | 8px | 卡片、Modal |
| XL | 12px | 统计卡片 |

---

## 四、各页面详细改版方案

### 4.1 全局布局（App.tsx）

#### 4.1.1 当前问题
- 顶栏标题使用 `Title level={5}`，与其他页面标题不一致
- 侧边栏 Logo 区的文字大小不统一
- 内容区 padding 使用 `24px`，但无统一变量

#### 4.1.2 改版方案

**顶栏 Header**：
- 左侧：折叠按钮 + 面包屑导航（当前页面名称）
- 右侧：显示当前日期 + 用户标识
- 标题使用统一字体规范

**侧边栏 Sider**：
- Logo 区域高度统一为 56px
- 菜单项 icon 统一 18px
- 选中项背景色使用浅色主题色

**内容区 Content**：
- 统一 padding 24px
- 背景色使用 `#f5f5f5`
- 所有页面内容卡片统一使用 `size="small"` 的 Card 包裹
- 页面标题统一在内容区顶部，样式一致

### 4.2 财务总览（Dashboard）

#### 4.2.1 当前问题
- 页面标题使用 `<h2>`，与其他页面不一致
- 统计卡片没有统一大小，部分使用 `size="small"` 部分默认
- 关键指标和风险评估使用 `size="small"` 但其他卡片默认
- 空状态只有文字，无视觉提示

#### 4.2.2 改版方案

**顶部统计卡片行**：
- 4张统计卡片（总资产、总负债、净资产、负债率）
- 统一使用 `size="small"`，固定高度
- 数值使用 24px/700 字重
- 添加微弱的 hover 上浮效果

**中部分析卡片**：
- 左侧"关键指标"：增加每个指标的图标，优化进度条展示
- 右侧"风险评估"：优化 Alert 的排版，增加风险等级标识

**底部图表区**：
- 统一饼图高度 260px
- 图例统一在底部，字号 12px
- 没有数据时显示统一的空状态组件

**高利率债务列表**：
- 增加列表项 hover 效果
- 每项添加利率进度条可视化
- 最多显示 5 条，点击"查看更多"跳转到债务管理

### 4.3 债务管理（DebtManager）

#### 4.3.1 当前问题
- 页面标题 `<h3>` 与其他页面不一致
- 总结栏中字号混用 16px / 14px
- 表格列"剩余金额"和"已用额度"内容重复
- 操作列 AssetManager 有文字，这里只有图标
- 导入/导出按钮组样式可以更紧凑

#### 4.3.2 改版方案

**顶部统计栏**：
- 统一使用统计卡片风格，显示总负债、总额度、已用、剩余
- 使用颜色区分正负值

**操作按钮组**：
- 统一使用 `Button` 带图标+文字形式
- 添加债务按钮改为 `type="primary"` 突出
- 导入/导出/模板按钮统一分组

**表格优化**：
- 删除"已用额度"列（与"剩余金额"重复）
- 优化"使用率"列，用进度条替代 Tag
- 操作列统一使用图标+文字（与 AssetManager 一致）
- 增加表格行 hover 高亮
- 优化列宽分配

**表单优化**：
- Modal 标题统一格式
- 表单字段增加 placeholder 提示
- 利率字段增加常见利率范围提示
- 提交成功后自动关闭并刷新列表

### 4.4 资产管理（AssetManager）

#### 4.4.1 当前问题
- 标题 font-size 18px 与其他页面不一致
- 操作列按钮带文字，但 DebtManager 操作列只有图标
- 表格无分页（合理，但与其他页面不一致）

#### 4.4.2 改版方案
- 统一统计栏风格
- 操作列与其他页面保持一致（图标+文字）
- 保持无分页（资产通常较少）
- 增加流动性可视化（进度条或图标）
- 空状态优化

### 4.5 收入支出（IncomeManager）

#### 4.5.1 当前问题
- 实时保存（onValuesChange）过于频繁
- 空状态处理不够优雅
- 预计还清时间在无数据时显示 "--"

#### 4.5.2 改版方案
- 改为手动保存模式，增加"保存"按钮
- 添加保存成功/失败反馈
- 增加实时预览区域（输入即显示计算结果）
- 优化统计卡片布局
- 增加支出分类占比分析

### 4.6 还款规划（PlanEngine）

#### 4.6.1 当前问题
- 策略选择与视图切换在同一行，信息层级混乱
- 无目标日期时空白区域无引导
- 策略对比分析使用进度条对比，视觉效果不够直观
- 明细模式的表格展开行样式不够美观

#### 4.6.2 改版方案
- 顶部操作栏重新布局：左侧标题，右侧策略选择和视图切换
- 统计卡片统一风格
- 策略对比改为柱状图对比，更直观
- 图表增加交互提示
- 目标日期区域增加引导提示
- 明细表格展开行优化样式，增加债务名称高亮

### 4.7 债务重组（DebtRestructure）

#### 4.7.1 当前问题
- Tabs 标签页图标+文字混排，部分 Tab 内容过少
- "借新还旧" Tab 只有静态说明，无交互功能
- 统计卡片参数不统一
- 参数设置与结果展示布局可优化

#### 4.7.2 改版方案
- 统一 Tabs 风格，优化 Tab 顺序（最低还款滚动→余额转移→债务整合→借新还旧）
- "借新还旧" 增加交互式模拟（输入金额、利率，模拟不同场景）
- 所有参数设置区统一 Card 包裹
- 结果展示区使用统一样式
- 增加"方案对比"视图，并排对比不同策略
- 风险提示优化，增加风险等级标识

---

## 五、交互与体验优化

### 5.1 统一交互规范

| 操作 | 交互方式 | 反馈 |
|------|---------|------|
| 添加 | Modal 弹窗 | 成功 Toast |
| 编辑 | Modal 弹窗（预填数据） | 成功 Toast |
| 删除 | Popconfirm 确认 | 成功 Toast |
| 保存 | 手动点击保存按钮 | 成功/失败 Toast |
| 导出 | 直接下载文件 | 成功 Toast |
| 导入 | 文件选择器 | 结果统计 Toast |

### 5.2 加载状态

- 页面首次加载：使用 Ant Design Skeleton 骨架屏
- 数据刷新：局部 Loading spinner
- 操作提交：按钮 loading 状态

### 5.3 空状态

统一空状态组件：
- 有图标（空列表、无数据等）
- 有标题
- 有引导操作按钮
- 不同模块使用不同图标

### 5.4 响应式布局

- 统计卡片：`xs={24} sm={12} md={6}` 统一响应式
- 图表：根据容器宽度自适应
- 表格：支持横向滚动
- Modal：宽度自适应，最大 600px

---

## 六、代码重构建议

### 6.1 样式抽取

- 抽取 `theme.ts` 定义全局主题变量
- 常用样式抽取为 CSS classes 或 styled-components
- 页面级样式内联，复用样式抽取

### 6.2 组件抽取

- `PageHeader`：统一页面标题+副标题组件
- `StatisticCard`：统一统计卡片组件
- `EmptyState`：统一空状态组件
- `PageContainer`：统一页面布局容器
- `ActionBar`：统一操作栏组件

### 6.3 主题配置

在 `main.tsx` 的 ConfigProvider 中完善主题配置：

```tsx
<ConfigProvider
  locale={zhCN}
  theme={{
    token: {
      colorPrimary: '#1677ff',
      borderRadius: 6,
      fontSize: 14,
      controlHeight: 36,
    },
    components: {
      Card: {
        paddingLG: 16,
      },
      Table: {
        headerBg: '#fafafa',
      },
    },
  }}
>
```

---

## 七、改版实施优先级

### P0 - 高优先级（核心体验）

1. 统一字体大小体系（全局）
2. 统一页面标题和布局规范（所有页面）
3. 统一卡片大小和间距（所有页面）
4. 统一统计卡片样式（所有页面）

### P1 - 中优先级（功能逻辑）

5. 统操作列按钮样式（DebtManager + AssetManager）
6. 收入支出改为手动保存
7. 修复债务管理表格重复列
8. 统一空状态处理

### P2 - 低优先级（体验优化）

9. 抽取全局样式和组件
10. 完善主题配置
11. 添加加载状态和骨架屏
12. 优化图表交互
13. 添加过渡动画和 hover 效果

---

## 八、技术实现方案

### 8.1 新建文件结构

```
src/
├── styles/
│   └── theme.ts              # 主题变量和设计令牌
├── components/
│   ├── Common/
│   │   ├── PageHeader.tsx    # 统一页面标题
│   │   ├── StatisticCard.tsx # 统一统计卡片
│   │   ├── EmptyState.tsx    # 统一空状态
│   │   └── ActionBar.tsx     # 统一操作栏
│   └── ... (已有组件)
```

### 8.2 ConfigProvider 主题配置

在 `main.tsx` 中完善 Ant Design 主题配置，统一全局字体、颜色、间距。

### 8.3 全局样式规范

- 所有页面标题统一使用 `PageHeader` 组件
- 统计数值统一使用 `StatisticCard` 组件
- 卡片统一使用 `size="small"`
- 操作按钮统一规范

---

## 九、改版后效果预览

### 9.1 页面统一规范

| 元素 | 改版前 | 改版后 |
|------|--------|--------|
| 页面标题 | 混用 h2/h3 | 统一 PageHeader 组件，20px/600 |
| 统计卡片 | 混用 size/无 size | 统一 StatisticCard，16px padding |
| 卡片大小 | 混用 small/middle | 统一 small |
| 操作按钮 | 图标/图标+文字 混用 | 统一图标+文字 |
| 空状态 | 纯文字 | 统一 EmptyState 组件 |
| 表头样式 | 无统一规范 | 统一 font-size 14px/600 |
| 间距 | 混用 16px/24px | 统一 16px（组件内）/ 24px（区块间） |

### 9.2 色彩语义化

- 红色（#ff4d4f）：仅用于高风险、逾期、负值
- 绿色（#52c41a）：仅用于正向、已还清、充足
- 蓝色（#1677ff）：仅用于主色、信息、链接
- 黄色（#faad14）：仅用于警告、注意